$(document).ready(function() {
	// Submit Record
    $('#frmAddEditThemes').submit(function(e) {
        e.preventDefault();
        let formData = $('#frmAddEditThemes').serialize();
        var type = "POST";
        var ajaxurl = $('#frmAddEditThemes').attr('action');
        $.ajax({
            type: type,
            url: ajaxurl,
            data: formData,
            dataType: 'json',
            success: function(data) {
                let res = data.response;
                if(res.status_code == 3009){
                    Swal.fire({
                        title: 'success',
                        text: res.message,
                        icon: 'success',
                        customClass: {
                          confirmButton: 'btn btn-primary'
                        },
                        buttonsStyling: false
                    });
                    // dt_basic.ajax.reload( null, false );
                } else if(res.status_code == 3010){
                    Swal.fire({
                        title: 'success',
                        text: res.message,
                        icon: 'success',
                        customClass: {
                          confirmButton: 'btn btn-primary'
                        },
                        buttonsStyling: false
                    });
                    // dt_basic.ajax.reload( null, false );
                } else {
                    Swal.fire({
                        title: 'Error!',
                        text: ' Something went wrong!',
                        icon: 'error',
                        customClass: {
                          confirmButton: 'btn btn-primary'
                        },
                        buttonsStyling: false
                    });
                }
            },
            error: function(data) {
                Swal.fire({
                    title: 'Error!',
                    text: ' Something went wrong!',
                    icon: 'error',
                    customClass: {
                      confirmButton: 'btn btn-primary'
                    },
                    buttonsStyling: false
                });
            }
        });
    });
})